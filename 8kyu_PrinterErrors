public class Printer {
    
    public static String printerError(String s) {
      int sum=0;
      int dl=s.length();
        for (int i=0; i<dl;i++){
          if(s.charAt(i)>109)
          sum++;}
        return String.valueOf(sum)+"/"+dl;
    }
}
